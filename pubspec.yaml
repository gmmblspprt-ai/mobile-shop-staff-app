name: mobile_shop_app
description: ระบบจัดการร้านมือถือ - Staff App (Shopee + Lazada + หน้าร้าน)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.2.0
  flutter_riverpod: ^2.5.1
  mobile_scanner: ^5.2.3
  dio: ^5.5.0+1
  shared_preferences: ^2.4.0
  intl: ^0.19.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
